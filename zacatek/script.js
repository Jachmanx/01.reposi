function getName() {
  const iUserName = document.getElementById("username").value;
    
  console.log(inputUserName)
  }

  switch(operator) {
    case '+':
      result = firstNumber + secondNumber;
      break;
    case '-':
      result = firstNumber - secondNumber;
      break;
    case '*':
      result = firstNumber * secondNumber;
      break;
    case '/':
      result = firstNumber / secondNumber;
      break;
  }

  function checkResult() {
    const inputNumber = parseInt(document.getEle
  }